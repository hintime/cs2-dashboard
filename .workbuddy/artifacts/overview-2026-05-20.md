# 2026-05-20 改动汇总

## 前端修复 (index.html)
1. **BUFF 折线图不显示** — `buff_history.json` 结构是 `{date: {name: {data}}}` 但代码按 `{name: [prices]}` 查，已修复
2. **持仓表格列顺序错位** — BUFF价和涨跌两列 header 与数据渲染顺序互换，已修复
3. **磨损截断** — "崭新出厂"被截成"崭新出"，加 `min-width` + `nowrap`
4. **收入统计区隐藏** — 无卖出记录时不再 `display:none`，改为显示空状态
5. **卖出记录双轨存储** — `localStorage`（即时响应）+ Worker API（KV 持久化）

## Cloudflare Worker (worker.js)
- **v2**: 新增 `GET/POST/DELETE /api/sold` KV 持久化接口
- **v3**: 实时查价双源兜底 → CSQAQ 优先，失败自动切 SteamDT
- 环境变量: `CSQAQ_API_TOKEN` + `STEAMDT_KEY`

## 部署
- 域名: `cs2wyx.asia`（Cloudflare 代理）
- Worker 绑定 KV: `SOLD_KV` → `CS2_SOLD`
- GitHub Pages 自动部署正常
