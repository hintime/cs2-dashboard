# 2026-09-19 工作日志

## 18:40 本轮 `all` 跑通核查（9c6a5f1 清尾巴 + 6bcb042 修 CSQAQ 排行榜）

结论：**6 项通过 / 1 项失败（排行榜 401 未消除）/ 无需回滚**。
本轮 all：17:13:40→18:31:55，用时 4695s，rc=0。

### 通过项
- firepulse 两处调用点恢复：`持仓精确化: 32/32 项`、`大盘历史: 共 7 天`，Traceback=0。
  旧日志里的 `大盘历史失败: too many values to unpack` 与
  `持仓精确化失败: enrich_items() got an unexpected keyword argument 'id_cache'` 本轮消失
  → 9c6a5f1 删重复定义确实生效。
- buy_fill：优先通道 30/30、存世量 29/30、合计 30/30。
- 推荐池：30 条，贴纸/胶囊残留 0，buff_buy>0 = 30/30，n_supply_real>0 = 30/30，
  分数 59.4~69.5（26 个不同值），tag = buff 23 / eco 7，三组恒等契约 30/30。
- 体检三件套全绿：proxy_field_audit（恒等 100%「✓ 契约成立」+「★ 无告警」）、
  rec_field_audit（死字段「无」）、lint（All checks passed）。
- 上线：两提交在 HEAD 祖先链上（`merge-base --is-ancestor` rc=0），rev-list `0 0`，
  站点 200，线上 market.json recommendations.all = 30。

### 失败项 + 根因（★ 重要）
排行榜接口仍 401：`CSQAQ rank p1..p4: HTTP Error 401`，`Rank list merged` 未打印。
但代码改动确已生效（日志格式 `price_up_1d p1`×8 → `rank p1`×4，符合"只取一次"）。

**真正根因不是接口契约，而是 token 注入缺失**：
`recommend.py:19` 用 `os.environ.get('CSQ_API_TOKEN','')`，而全仓库无任何启动脚本注入该
环境变量（grep 非日志文件，只有 `local_keys.env`、`buy_fill.py`、`csqaq_buy.py`、`server.py`
与文档提到）。反证：同轮 `[BUY] CSQAQ 优先通道: 30/30` 成功，因为 `buy_fill.py:231`
直接读 `local_keys.env`（token 24 字符）。全量查价 batch 401 同源。

**修复方向**：给 `recommend.py` 加 `local_keys.env` 回退取值，**不要回滚 6bcb042**。

### 历史遗留（非本轮回归）
`[NAME] name_map generation skipped: cannot access local variable 'json'` —— 9-15 起每轮都有。
