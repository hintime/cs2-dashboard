# 2026-09-20 工作日志

## 13:2x 只读核验：be0815c「CSQ_API_TOKEN 加载顺序」修复是否在生产生效

结论：**修复已生效，无新增阻断问题，不需回滚。**

### 核查对象
- 轮次：updater 于 00:35:33 拉起 `update.py all`，01:52:57 结束，rc=0，用时 4643 秒。
  （任务描述中的"00:31"实为 `update.py prices` 轮：00:31:13→00:35:03。）
- 日志：logs/run_all_20260920_003534.log（285 行）。
- 代码侧确认：update.py L35-44 加载 local_keys.env 在 L62 `import recommend` 之前；
  recommend.py L21-47 有 `_load_local_keys()` 兜底 + 空 key 快速失败；`be0815c` 是 HEAD 祖先。

### 关键证据（对比修复前 09-16~09-19）
| 指标 | 修复前 | 本轮 |
|---|---|---|
| Batches | 0/4789（401=3） | 4790/4792（401=0, other_fail=0） |
| 恢复备份 | 每轮出现 | 未出现 |
| Batch merged | 无 | 4790 items，独立基准价 steam_sell 4529 |
| BUY 合计 | 30/30（靠兜底） | 30/30（CSQAQ 优先 30/30 + SteamDT 兜底） |

- 日志中唯一含 "401" 的行是 `401=0` 本身；`HTTP Error 401` 实际 0 行。

### 体检
- lint.py：All checks passed
- normalize.py --self-test：8/8 通过
- proxy_field_audit.py：★ 无告警，三组恒等契约全部成立

### market.json（已提交版 27795f2 @01:55，与工作区一致）
- 30 条、贴纸/胶囊残留 0、buff_sell>0 30/30、n_steam>0 28/30
  （n_steam=0：沙漠之鹰|东方之谜(久经)、USP消音版|眩流(略有磨损)）
- tag：buff 25 / eco 5；score 59.80~72.90，28 个唯一值；无重复 hash_name

### 线上
- https://cs2wyx.asia/ → 200，正文真实 HTML
- https://cs2wyx.asia/market.json → 200，正文 JSON（非 404 改写），recommendations.all=30，
  updated=2026-09-19T17:55:13Z（= 北京 09-20 01:55:13，即本轮 all 之后的 prices 轮）

### 遗留 / 新问题（均非本次修复引入）
1. `[CSQAQ] Rank list merged: 0 items` —— 接口已返回数据（token 生效），
   但 update.py L3727 用 `a['name']` 建索引、L3731 用 `HashName` 查表，名称对不上 → merged=0。
   不影响结果（全量扫描已覆盖 4790 件），但任务里"N>0"的判据不成立。
2. `[NAME] name_map generation skipped: cannot access local variable 'json'`（09-16 起存在）
3. `[AI] ollama HTTP 500`（09-18 起存在，本轮走 fallback mode）
4. **调度疑似中断**：updater 日志 02:10:24 后停写约 11 小时；13:24:07 又起新一轮 all
   （run_all_20260920_132407.log，当前为空，2 个 python 进程在跑）。
5. **未推送提交**：本地 HEAD 7976cc8「wip: local snapshot 2026-09-20 13:24」领先远端 27795f2 一个提交（仅改 price_summary.json）。
