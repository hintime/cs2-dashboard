# CS2 看板 · 本机调度器 —— 最后一步（需你手动执行）

## 为什么需要你手动执行

沙箱的安全策略把 `schtasks.exe` 列入了**程序黑名单**，我无法创建设置计划任务
（错误明确要求"不要重试、不要换 shell 绕过"）。这一步只能由你在**普通命令行**
（无需管理员，因为是当前用户任务）里跑一下。

## 一条命令搞定

在文件资源管理器里**双击**这个文件即可：

```
C:\Users\Lenovo\cs2-runner-local\install_updater_task.bat
```

它会做三件事（末尾会 `pause`，你能看到结果）：

| 任务名 | 触发 | 作用 |
|---|---|---|
| `CS2-Updater-AtLogon` | 登录后延迟 1 分钟 | 启动常驻调度器 |
| `CS2-Updater-Watchdog` | 每 15 分钟 | 巡检，进程掉线自动拉起 |

同时会**立即启动一次**调度器，所以跑完就开始工作了。

## 如果想手动敲命令

```
schtasks /Create /TN "CS2-Updater-AtLogon" /TR "\"C:\Users\Lenovo\AppData\Local\Programs\Python\Python312\pythonw.exe\" \"C:\Users\Lenovo\cs2-runner-local\updater_daemon.py\"" /SC ONLOGON /DELAY 0001:00 /F

schtasks /Create /TN "CS2-Updater-Watchdog" /TR "cmd /c \"\"C:\Users\Lenovo\AppData\Local\Programs\Python\Python312\pythonw.exe\" \"C:\Users\Lenovo\cs2-runner-local\updater_daemon.py\" --watchdog\"" /SC MINUTE /MO 15 /F
```

## 验证是否装好

```
python "C:\Users\Lenovo\cs2-runner-local\updater_daemon.py" --status
```

看日志：

```
C:\Users\Lenovo\cs2-runner-local\logs\
```

## 调度周期（已内置）

| 周期 | 内容 | 是否调 AI |
|---|---|---|
| 每 30 分钟 | 价格线 | 否（`SKIP_AI=1`，省钱） |
| 每 2 小时 | 行情指数 | 否 |
| 每 6 小时 | 全量 + AI 深度分析 | 是 |

## 卸载

```
C:\Users\Lenovo\cs2-runner-local\uninstall_updater.bat
```

## 还有一件小事（可选）

建议在 GitHub 仓库补一个 Actions Secret：**`CSQ_API_TOKEN`**

- 本机 `local_keys.env` 里已有，所以本机调度不受影响
- 但**缺失会让 Steam 独立基准价 `n_steam` / `n_dev_steam` 为空**

路径：仓库 → Settings → Secrets and variables → Actions → New repository secret
