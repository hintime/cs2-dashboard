# 任务摘要：UI 布局调整 - 总成本移至持仓明细上方

## 目标
用户建议将「总成本」（投入本金）从顶部 KPI 条移至「持仓明细」板块上方，作为持仓板块的汇总信息。

## 修改内容

### 1. KPI 条调整
- 从 3 列改为 2 列
- 仅显示「总价值」和「总盈亏」
- 移除「总成本」

### 2. 持仓明细上方新增汇总条
- 显示「投入本金」标签和「总成本」数值
- 使用等宽字体，样式与 KPI 条一致

### 3. JavaScript 同步更新
- updateKPI() 函数新增对 `totalCostHeader` 元素的更新
- 确保总成本数值实时同步

## 文件变更
- `index.html` — 调整 KPI 条布局，新增持仓汇总条，更新 updateKPI() 函数

## 推送状态
- Commit: `d1945f2`
- Message: `ui: 将总成本移至持仓明细上方，KPI条仅显示总价值/总盈亏`
- 已推送至 `origin/main`
