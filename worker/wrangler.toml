name = "steamdt-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# 环境变量（在 Cloudflare Dashboard 中设置，或用命令行：
# npx wrangler secret put STEAMDT_API_KEY
# 然后输入你的 SteamDT API Key）

[vars]
# 默认值（会被 secret 覆盖）
# STEAMDT_API_KEY = "请在 Cloudflare Dashboard 设置"
