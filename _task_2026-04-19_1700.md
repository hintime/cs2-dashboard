# 任务摘要：持仓汇总区域整合 + 导航栏拉伸

## 目标
1. 将所有三个指标（总价值、总盈亏、投入本金）都放在持仓明细面板上方
2. 将4个导航标签拉伸占满一整行

## 修改内容

### 1. 移除顶部 KPI 条
- 删除 `kpi-strip` 块
- 清理相关 DOM 对象引用

### 2. 重构持仓汇总区域
- 将持仓明细上方的汇总条改为三列布局
- 显示：总价值、总盈亏、投入本金
- 每个指标卡片包含：标签、数值、副信息

### 3. 导航栏拉伸占满整行
- 将 `.tabs` 改为 `display:grid;grid-template-columns:repeat(4,1fr);width:100%`
- 调整 panel-head 布局为 flex-direction: column
- 标题和导出按钮在上方一行，tabs 独占下方一行

### 4. 更新 JavaScript
- 重写 `updateKPI()` 函数
- 使用 `getElementById` 获取新元素
- 移除对已删除 DOM 对象的引用

## 文件变更
- `index.html` — 布局重构，CSS 更新，JavaScript 更新

## 推送状态
- Commit 1: `b77dcdd` - 将总价值/总盈亏/投入本金都移至持仓明细上方
- Commit 2: `237ae21` - 持仓面板内tabs占满整行（错误位置）
- Commit 3: `4807089` - 顶部4个主导航按钮拉伸占满整行
- Commit 4: `a3c6d6b` - 导航按钮按板块顺序排列(异动→推荐→持仓→分析)
- Commit 5: `22fe4cb` - 修复导航按钮高亮状态切换bug
- 已推送至 `origin/main`
